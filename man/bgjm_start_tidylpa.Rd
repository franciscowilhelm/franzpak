% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callr_wrappers.R
\name{bgjm_start_tidylpa}
\alias{bgjm_start_tidylpa}
\title{Start a tidyLPA background job}
\usage{
bgjm_start_tidylpa(
  data,
  n_profiles,
  ...,
  package = c("mclust", "Mplus"),
  job_name = NULL,
  output_base = tempdir(),
  seed = NULL,
  return_strategy = c("inline", "rds"),
  pass_data_as = c("object", "rds"),
  packages = c("tidyLPA")
)
}
\arguments{
\item{data}{A data frame passed to \code{estimate_profiles()}.}

\item{n_profiles}{Number of profiles to estimate.}

\item{...}{Additional arguments forwarded to \code{estimate_profiles()}, such as
\code{models}, \code{variances}, \code{covariances}, etc. See tidyLPA documentation for details.}

\item{package}{\code{"mclust"} or \code{"Mplus"} backend indicator (tidyLPA >= 1.0 naming).}

\item{job_name}{Optional friendly job name.}

\item{output_base}{Directory where job folders should be created.}

\item{seed}{Optional random seed for reproducibility.}

\item{return_strategy}{Return result inline or as serialized RDS.}

\item{pass_data_as}{Whether to pass the data object or a temporary RDS path.}

\item{packages}{Additional packages to load in the child session.}
}
\value{
Invisible job identifier string.
}
\description{
Creates a background session that evaluates \code{\link[tidyLPA:estimate_profiles]{tidyLPA::estimate_profiles()}}
with the supplied data and configuration.
}
\details{
When \code{package = "Mplus"} you must have Mplus and
\code{MplusAutomation} available. Any files written by the child session are
moved to the job's \code{artifacts} directory.

This function uses the tidyLPA >= 1.0 API. By default, models are constructed
from \code{variances} and \code{covariances} arguments (passed via \code{...}). You can also
explicitly specify \code{models} (e.g., \code{models = 1} or \code{models = c(1, 2, 3)}) via \code{...}.
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
\dontrun{
# Using variances and covariances to specify model
job_id <- bgjm_start_tidylpa(
  data = iris[, 1:4],
  n_profiles = 3,
  variances = c("equal", "equal"),
  covariances = c("zero", "zero"),
  package = "mclust",
  job_name = "iris-demo"
)

bgjm_poll(0)
}
\dontshow{\}) # examplesIf}
}
