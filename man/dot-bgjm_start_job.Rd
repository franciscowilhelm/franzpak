% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callr_wrappers.R
\name{.bgjm_start_job}
\alias{.bgjm_start_job}
\title{Launch a background job}
\usage{
.bgjm_start_job(
  fun_expr,
  args = list(),
  job_name = NULL,
  output_base = tempdir(),
  packages = character(),
  libpath = .libPaths(),
  env = rcmd_safe_env(),
  seed = NULL,
  return_strategy = c("inline", "rds")
)
}
\arguments{
\item{fun_expr}{A quoted function definition to be evaluated in the child
process. This must return a function object.}

\item{args}{Named list with arguments forwarded to the target function.}

\item{job_name}{Optional friendly name for the job; defaults to the job id.}

\item{output_base}{Directory where per-job folders will be created.}

\item{packages}{Character vector of packages to load in the child process.}

\item{libpath}{Library paths passed to \code{\link[callr:r_bg]{callr::r_bg()}}.}

\item{env}{Named character vector of environment variables for the child
(defaults to \code{\link[callr:rcmd_safe_env]{callr::rcmd_safe_env()}}).}

\item{seed}{Optional integer seed forwarded to \code{\link[=set.seed]{set.seed()}} inside the job.}

\item{return_strategy}{Either \code{"inline"} to return results through IPC or
\code{"rds"} to persist the result as an \code{.rds} file.}
}
\value{
Invisible job identifier string.
}
\description{
Utility used by the higher-level wrappers to start work in a separate
R session via \code{\link[callr:r_bg]{callr::r_bg()}}. The job is registered in a package-local
environment so that it can later be polled or collected.
}
\keyword{internal}
