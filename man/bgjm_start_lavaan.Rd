% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bgjm.R
\name{bgjm_start_lavaan}
\alias{bgjm_start_lavaan}
\title{Start a lavaan background job}
\usage{
bgjm_start_lavaan(
  model_syntax,
  data,
  lavaan_fun = c("sem", "cfa", "lavaan"),
  ...,
  job_name = NULL,
  output_base = tempdir(),
  seed = NULL
)
}
\arguments{
\item{model_syntax}{Character vector containing lavaan model syntax.}

\item{data}{Data frame passed to the lavaan function.}

\item{lavaan_fun}{One of \code{"sem"}, \code{"cfa"}, or \code{"lavaan"}.}

\item{...}{Additional arguments forwarded to the selected lavaan function
(e.g. \code{se = "bootstrap"}, \code{bootstrap = 1000}).}

\item{job_name}{Optional friendly job name.}

\item{output_base}{Directory where the job folder is created. Defaults to
R's \code{tempdir()}, which is \strong{cleared when R exits} -- pass a persistent
path (e.g. inside your project) to keep the job's logs and artifacts.}

\item{seed}{Optional random seed. This seeds the job's \emph{main process} only
and does \strong{not} make a parallel bootstrap reproducible; for that, pass
lavaan's own \code{iseed} through \code{...}.}
}
\value{
Invisibly, a job identifier string -- a short handle, \strong{not} the
fitted model. Assign it and pass it to \code{\link[=bgjm_result]{bgjm_result()}} (or
\code{\link[=bgjm_collect]{bgjm_collect()}}) to retrieve the model once the job has finished.
}
\description{
Runs one of \code{lavaan::sem()}, \code{lavaan::cfa()}, or \code{lavaan::lavaan()} with the
provided syntax and data on a background daemon.
}
\examples{
\dontshow{if (interactive() && requireNamespace("lavaan", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontrun{
job <- bgjm_start_lavaan(
  model_syntax = "f1 =~ x1 + x2 + x3",
  data = lavaan::HolzingerSwineford1939,
  lavaan_fun = "cfa"
)
fit <- bgjm_collect(job)$result
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=bgjm_collect]{bgjm_collect()}}, \code{\link[=bgjm_status]{bgjm_status()}}
}
