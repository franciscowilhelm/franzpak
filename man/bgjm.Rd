% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bgjm.R
\name{bgjm}
\alias{bgjm}
\title{Background job registry for franzpak (mirai engine)}
\description{
These helpers run potentially long-running model fits in background R
processes using the \pkg{mirai} framework, while adding a thin layer that
\pkg{mirai} does not provide on its own:
}
\details{
\itemize{
\item a \strong{named registry} so jobs can be listed, polled, and collected by a
friendly name rather than by holding \code{mirai} objects directly, and
\item \strong{per-job filesystem management} -- each job gets its own directory, its
stdout/stderr are captured to log files, and any files written during the
run (e.g. Mplus \code{.inp}/\code{.dat}/\code{.out}) are relocated into an \verb{artifacts/}
sub-directory.
}

Execution, scheduling, cancellation, and timeouts are delegated to
\pkg{mirai}. Jobs run on a dedicated daemon pool (compute profile
\code{"franzpak"}) that is started lazily on first use; see \code{\link[=bgjm_daemons]{bgjm_daemons()}}.
}
\keyword{internal}
