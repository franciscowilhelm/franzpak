% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bgjm.R
\name{bgjm_result}
\alias{bgjm_result}
\title{Collect only the result object of a background job}
\usage{
bgjm_result(job_id, auto_remove = FALSE)
}
\arguments{
\item{job_id}{Identifier returned by a \verb{bgjm_start_*()} function.}

\item{auto_remove}{If \code{TRUE}, remove the job (and its directory) from the
registry after collection.}
}
\value{
The job's result object.
}
\description{
Convenience wrapper around \code{\link[=bgjm_collect]{bgjm_collect()}} that returns just the job's
result -- the fitted model or object -- without the surrounding metadata
list. Use this to avoid the easy-to-miss \code{bgjm_collect(job)$result} idiom.
}
\details{
Like \code{\link[=bgjm_collect]{bgjm_collect()}} it \strong{blocks} until the job resolves (mirroring mirai's
\code{m[]}) and raises an informative error if the job failed. When you also need
the artifacts directory or log paths, use \code{\link[=bgjm_collect]{bgjm_collect()}} instead.
}
\examples{
\dontshow{if (interactive() && requireNamespace("lavaan", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontrun{
job <- bgjm_start_lavaan("f1 =~ x1 + x2 + x3",
                         lavaan::HolzingerSwineford1939, "cfa")
fit <- bgjm_result(job, auto_remove = TRUE)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=bgjm_collect]{bgjm_collect()}}
}
