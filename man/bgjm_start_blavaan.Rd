% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bgjm.R
\name{bgjm_start_blavaan}
\alias{bgjm_start_blavaan}
\title{Start a blavaan (Bayesian SEM) background job}
\usage{
bgjm_start_blavaan(
  model_syntax,
  data,
  blavaan_fun = c("bsem", "bcfa", "bgrowth", "blavaan"),
  ...,
  job_name = NULL,
  output_base = tempdir(),
  seed = NULL
)
}
\arguments{
\item{model_syntax}{Character vector containing lavaan/blavaan model syntax.}

\item{data}{Data frame passed to the blavaan function.}

\item{blavaan_fun}{One of \code{"bsem"}, \code{"bcfa"}, \code{"bgrowth"}, or \code{"blavaan"}.}

\item{...}{Additional arguments forwarded to the selected blavaan function
(e.g. \code{n.chains}, \code{burnin}, \code{sample}, \code{target}, \code{mcmcfile}, \code{bcontrol},
\code{seed}).}

\item{job_name}{Optional friendly job name.}

\item{output_base}{Directory where the job folder is created. Defaults to
R's \code{tempdir()}, which is \strong{cleared when R exits} -- pass a persistent
path (e.g. inside your project) to keep the job's logs and artifacts.}

\item{seed}{Optional random seed for the job's main process. For reproducible
\emph{sampling}, pass the sampler seed through \code{...} (blavaan's \code{seed}) instead;
\code{seed} here does not control the MCMC streams.}
}
\value{
Invisibly, a job identifier string -- a short handle, \strong{not} the
fitted model. Assign it and pass it to \code{\link[=bgjm_result]{bgjm_result()}} (or
\code{\link[=bgjm_collect]{bgjm_collect()}}) to retrieve the model once the job has finished.
}
\description{
Runs one of \code{blavaan::bsem()}, \code{blavaan::bcfa()}, \code{blavaan::bgrowth()}, or
\code{blavaan::blavaan()} with the provided syntax and data on a background daemon.
Because blavaan fits are MCMC and typically slow, they are a natural fit for
background execution.
}
\details{
The inner parallelism here is the number of MCMC \strong{chains}. By
default blavaan runs chains sequentially; pass \code{bcontrol = list(cores = n.chains)} (Stan backend) to run them in parallel, which works inside a
background job and gives close to a linear speed-up. Mind the core budget:
a job using \code{cores = k} consumes \code{k} cores, so size the daemon pool so that
\verb{workers x cores-per-job} stays within your CPU count. See the package's
background-jobs vignette ("Parallelism and CPU budgeting").

With \code{mcmcfile = TRUE}, blavaan writes the Stan/JAGS model and data to disk;
those files are captured into the job's \verb{artifacts/} directory.
}
\examples{
\dontshow{if (interactive() && requireNamespace("blavaan", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\dontrun{
job <- bgjm_start_blavaan(
  model_syntax = "f1 =~ x1 + x2 + x3",
  data = lavaan::HolzingerSwineford1939,
  blavaan_fun = "bcfa",
  n.chains = 2, burnin = 500, sample = 500
)
fit <- bgjm_result(job)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=bgjm_start_lavaan]{bgjm_start_lavaan()}}, \code{\link[=bgjm_collect]{bgjm_collect()}}
}
